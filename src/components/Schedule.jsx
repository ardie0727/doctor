import React from 'react';
import Layout from './Layout'; // Import Layout component

const Schedule = () => {
  return (
    <Layout>
      <h1 className="text-3xl font-semibold">Schedule</h1>
      {/* Add schedule-related content here */}
    </Layout>
  );
};

export default Schedule;
